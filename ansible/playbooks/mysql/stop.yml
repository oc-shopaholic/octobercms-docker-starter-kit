---
- name: 'Stop MySQL docker container'
  hosts: localhost
  tasks:
  - name: 'Include global vars'
    include_vars:
      dir: ./../../vars/global
  - name: 'Stop container'
    docker_container:
      name: mysql-{{ PROJECT_NAME }}
      image: "mysql:{{ MYSQL_VERSION }}"
      state: stopped